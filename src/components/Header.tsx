// src/components/Header.tsx
// Importamos el NavBar que acabamos de modificar
import { NavBar } from "./NavBar";

export const Header = () => {
return (
    <header>
    <NavBar />
    </header>
);
}